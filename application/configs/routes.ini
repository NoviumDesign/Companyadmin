routes.orders.route = orders/:business
routes.orders.defaults.controller = orders
routes.orders.defaults.action = index
routes.orders.defaults.business = all
routes.orders.reqs.business = \w+

routes.order.route = order/:id
routes.order.defaults.controller = order
routes.order.defaults.action = index
routes.order.defaults.id = 1
routes.order.reqs.id = \d+


routes.products.route = products/:business
routes.products.defaults.controller = products
routes.products.defaults.action = index
routes.products.defaults.business = all
routes.products.reqs.business = \w+

routes.product.route = product/:id
routes.product.defaults.controller = product
routes.product.defaults.action = index
routes.product.defaults.id = 1
routes.product.reqs.id = \d+